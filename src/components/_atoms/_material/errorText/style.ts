import {StyleRulesCallback} from 'material-ui/styles/withStyles';

export const styleErrorText: StyleRulesCallback<'root'> = (theme) => {
  return {
    root: {
      marginTop: theme.spacing.unit,
    },
  };
};
